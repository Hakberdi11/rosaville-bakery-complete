# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

`AGENTS.md` in this directory describes the original Base44 platform workflow (`base44 dev`, publish flow) — **that no longer applies**. Base44 has been fully removed from this app; see the root `.claude/rules/ecosystem.md` and `.claude/rules/admin-dashboard.md` for the current architecture (a plain Vite/React SPA calling the shared Django backend in `rosaville-backend/`) and commands.
